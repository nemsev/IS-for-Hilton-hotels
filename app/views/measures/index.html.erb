<h1>Справочник единиц измерения</h1>

<table>
  <tr>
	<th>ID</th>
    <th>Единица измерения</th>
    <th></th>
    <th></th>
  </tr>

<% @measures.each do |measure| %>
  <tr>
	<td><%= measure.id %></td>
    <td><%= measure.name %></td>
    <td><%= link_to 'Редактировать', edit_measure_path(measure) %></td>
    <td><%= link_to 'Удалить', measure, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новая единица измерения', new_measure_path %>
